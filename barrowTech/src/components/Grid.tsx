import React from 'react';
import './Grid.css';

interface GridProps {
  rows: number;
  columns: number;
}

const Grid: React.FC<GridProps> = () => {
  return (
    <body>

    </body>
  );
};

export default Grid;
